name: state_forge_devtools
description: Source app for the StateForge DevTools extension bundled with the state_forge package.
publish_to: 'none'
version: 0.1.7

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter
  devtools_app_shared: ^0.5.0
  devtools_extensions: ^0.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
